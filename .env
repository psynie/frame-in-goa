# Absolute base URL of the deployed site, with a trailing slash.
# The OG tags in index.html build the og:image URL from this, so X link
# previews break if it does not match where the site is actually served.
VITE_SITE_URL=https://psynie.github.io/frame-in-goa/
